# ════════════════ ProLine CAD — Pinned Dependencies ════════════════
# 由 pyproject.toml 生成，用于 CI/CD 和 Docker 构建的确定性安装
# 更新方式: pip-compile pyproject.toml -o requirements.txt

# ── Core ──
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
psycopg2-binary>=2.9.9
kafka-python>=2.0.2
minio>=7.2.0
httpx>=0.27.0
python-multipart>=0.0.9

# ── Agent1: ParseAgent ──
ezdxf>=1.3.0
Rtree>=1.2.0
Shapely>=2.0.4
rdflib>=7.0.0

# ── Agent2: ConstraintAgent ──
z3-solver>=4.12.0

# ── Agent3: LayoutAgent ──
numpy>=1.26.0
scipy>=1.13.0
deap>=1.4.1

# ── LLM Integration ──
openai>=1.30.0
tiktoken>=0.7.0
pymilvus>=2.4.0

# ── Dev Tools ──
pytest>=8.2.0
pytest-cov>=5.0.0
pytest-asyncio>=0.23.0
ruff>=0.4.0
mypy>=1.10.0
pre-commit>=3.7.0

# ── Dashboard backend (T2 W2 / M1) ──
sqlalchemy>=2.0.30
alembic>=1.13.1
geoalchemy2>=0.15.0
structlog>=24.1.0
opentelemetry-sdk>=1.25.0
opentelemetry-api>=1.25.0
opentelemetry-instrumentation-fastapi>=0.46b0
opentelemetry-instrumentation-sqlalchemy>=0.46b0
prometheus-client>=0.20.0
pytest-timeout>=2.3.0

# ── Dashboard backend (T2 W2 / M2: queue + uploads + auth) ──
arq>=0.26.0
redis>=5.0.0
python-magic-bin>=0.4.14; platform_system == "Windows"
python-magic>=0.4.27; platform_system != "Windows"
python-jose[cryptography]>=3.3.0
email-validator>=2.0.0
