# ════════════════ Python ════════════════
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
dist/
build/
eggs/
*.whl

# ════════════════ Virtual Environment ════════════════
.venv/
venv/
ENV/

# ════════════════ IDE / Editor ════════════════
# Ignore per-user VS Code state but keep shared workspace files committed.
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~
.project
.classpath
.settings/

# ════════════════ Testing ════════════════
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/
coverage.xml
*.cover

# ════════════════ Type Checking ════════════════
.mypy_cache/
.pytype/

# ════════════════ Environment / Secrets ════════════════
# Per CLAUDE.md §8: secrets only via env vars; never commit any .env* file.
.env
.env.*
.env.local
.env.*.local
*.env
*.local
*.local.*
*.secret
*.secret.*

# ════════════════ OS ════════════════
.DS_Store
Thumbs.db
desktop.ini

# ════════════════ Docker ════════════════
docker-compose.override.yml

# ════════════════ Storage / Data (runtime) ════════════════
*.log
logs/
data/
*.db
*.sqlite3

# ════════════════ Docker Volumes (local dev, per ADR-0007) ════════════════
# All docker-compose bind-mount volumes live here — wipeable, rebuildable
# via `scripts/init_minio.py` + `scripts/sync_corpus_to_minio.py`.
/.data/

# ════════════════ Constraint Corpus Blobs (copyrighted / large) ════════════════
# Per ADR-0007: Git tracks metadata.yaml and ≤2KB clauses/*.txt only;
# binary PDFs / full-text go to MinIO via sync scripts.
/corpus/**/blobs/


# ════════════════ Build Artifacts ════════════════
node_modules/
*.min.js
*.min.css

# ════════════════ Secrets ════════════════
*.pem
*.key
*.cert
secrets/

# ════════════════ Runtime artifacts ════════════════
exp/
tools/
dump_*.txt
*.log
offload_temp/


# ════════════════ Local scratch ════════════════
tmp_quarantine/
tmp_review.csv

